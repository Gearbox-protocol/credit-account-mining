enum ActionType {
  GOTO_ROOT = 'GOTO_ROOT',
  GOTO = 'GOTO',
  COMMAND = 'COMMAND',
  HELP = 'HELP',
  JOIN = 'JOIN',
  JOIN_ACCEPTED = 'JOIN_ACCEPTED',
  JOIN_DENIED = 'JOIN_DENIED',
  MINED = 'MINED',
  LINKS = 'LINKS',
}

export default ActionType;
