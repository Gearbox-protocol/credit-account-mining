enum ActionType {
  NEXT = 'NEXT',
  GOTO_ROOT = 'GOTO_ROOT',
  GOTO_JOIN = 'GOTO_JOIN',
  COMMAND = 'COMMAND',
  HELP = 'HELP',
  JOIN = 'JOIN',
  JOIN_ACCEPTED = 'JOIN_ACCEPTED',
  JOIN_DENIED = 'JOIN_DENIED',
  MINED = 'MINED',
  LINKS = 'LINKS',
}

export default ActionType;
